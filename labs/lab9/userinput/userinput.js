//get the data from the html file made
let txtName = document.querySelector("#txtName");
// the function that makes the code say hello to user
function sayHello() {
// display hello then the value of txtName  as well with + txtName 
	console.log("Hello " + txtName.value);
}
